# Assets

`development/` is reserved for clearly marked development placeholders. `release/` may only contain assets with an approved entry in `provenance/asset-registry.json`. The asset audit fails when a releasable binary lacks provenance or commercial-use approval.
