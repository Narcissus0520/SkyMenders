# AGENTS.md — Project Skyforge（内部代号：浮岛工坊）

> 本文件是仓库根目录的最高级工程执行规范，适用于 Codex、其他代码智能体以及参与本项目的开发者。  
> 除非用户明确修改，本文件中的产品边界、架构原则、合规约束和验收标准均为强制要求。

---

## 0. 文档状态

- **项目类型**：微信小游戏完整产品，不是技术 Demo、玩法原型或美术样片。
- **产品品类**：原创 2D 横屏回合制弹道工程战术肉鸽。
- **内部代号**：Project Skyforge / 浮岛工坊。
- **公开名称**：尚未最终确定；完成商标、小游戏重名及近似名称检索前，不得把内部代号直接视为正式品牌。
- **当前目标**：
  1. 先交付可独立公开发布和长期游玩的完整 PvE V1.0。
  2. V1.0 稳定后，在同一确定性战斗内核上交付实时 PvP V2.0。
- **当前商业化**：无广告、无内购、无付费通行证、无抽卡、无现金交易。
- **当前动画边界**：不制作剧情过场、视频 CG、配音口型或复杂角色演出；仍必须实现操作反馈、战斗反馈、UI 动效、镜头运动和基础程序化角色动作。
- **首发语言**：简体中文；所有文本从第一天起使用国际化 Key，不允许直接把中文硬编码进业务逻辑。
- **首发平台**：微信小游戏，横屏。
- **主要技术栈**：
  - Cocos Creator 3.8 LTS + TypeScript。
  - Node.js 24 LTS。
  - NestJS 11，模块化单体，默认 Fastify Adapter。
  - PostgreSQL + Redis。
  - React + TypeScript + Vite 内容编辑器和管理后台。
  - pnpm Workspace + Turborepo。
  - Docker Compose 起步，保留向 Kubernetes 迁移的边界。
- **法律声明**：本文件提供工程合规约束，不替代律师、出版单位、平台审核人员或主管部门的专业意见。任何公开发布都必须重新核验当时有效的法律法规、微信平台规则、游戏审批要求和主体资质。

---

# 1. Codex 的使命

Codex 的任务不是搭建脚手架、实现一个弹道样例或留下大量 TODO，而是持续推进并完成一个结构清晰、可测试、可部署、可维护、可发布的完整项目。

Codex 必须：

1. 先阅读本文件、现有代码、现有文档和当前状态记录，再修改代码。
2. 按阶段实现，但始终维护完整产品架构，禁止用临时代码堵住核心路径。
3. 每完成一个阶段，运行本文件规定的测试和验收命令。
4. 持续更新：
   - `docs/status/PROJECT_STATUS.md`
   - `docs/status/IMPLEMENTATION_PLAN.md`
   - `docs/status/KNOWN_ISSUES.md`
   - `docs/adr/`
5. 对日常工程选择自主决策，不反复向用户询问低价值细节。
6. 遇到外部凭证、主体资质、正式美术或正式音频缺失时：
   - 实现完整接口、校验、占位流程和导入机制；
   - 在 `docs/status/EXTERNAL_BLOCKERS.md` 中精确记录；
   - 继续完成不依赖该外部条件的工作；
   - 不得把占位素材或 Mock 服务宣称为正式完成。
7. 不得擅自推送远端、发布生产环境、提交微信审核、创建收费资源或购买第三方服务。
8. 不得删除或覆盖用户已有修改；发现冲突时保留用户内容并记录差异。
9. 不得因为项目规模较大而退化成 Demo。
10. 不得在核心逻辑中留下未跟踪的 `TODO`、`FIXME`、空实现或“后续再补”。

---

# 2. 不可妥协的产品原则

## 2.1 原创性

本项目可以属于“回合制弹道战术游戏”这一通用品类，但不得复制、仿制或暗示关联任何现有商业游戏。

尤其禁止：

- 在公开名称、描述、关键词、宣传图、代码资源名中使用“百战天虫”“Worms”“微信版百战天虫”“国产 Worms”等表述。
- 使用虫状、蚯蚓状或与相关知名角色高度近似的角色轮廓、五官比例、动作和性格表达。
- 复制或高度模仿标志性武器名称、造型、音效、台词、爆炸演出、胜利动作、UI 布局和地图构图。
- 使用第三方游戏截图、角色、Logo、字体、音乐、音效或未经授权的二创素材。
- 以“致敬”为理由规避素材授权或相似性审查。
- 让生成式 AI 模仿具名在世艺术家、具体商业游戏或可识别品牌的视觉风格。
- 将竞品资源放入发布包、宣传仓库或公开演示。

允许在 `docs/legal/COMPETITOR_DIFFERENTIATION.md` 中以文字方式记录品类差异和风险审查，但不得存放竞品素材。

## 2.2 隐私最小化

V1.0 默认不申请、收集或使用：

- 通讯录。
- 精确位置。
- 麦克风。
- 摄像头。
- 相册。
- 手机号。
- 自定义头像。
- 自由文本昵称。
- 微信真实昵称或微信头像。
- 文本聊天。
- 语音聊天。
- 玩家地区展示。
- 广告标识。
- 跨应用追踪标识。

账户仅使用微信登录所必需的服务端交换流程。`AppSecret`、平台密钥和服务端凭证永远不得进入客户端。

## 2.3 公平性

- 局外成长只能扩展策略选择，不能永久提高基础生命、基础伤害、能源上限、命中率或减伤。
- 每日挑战固定阵容、固定模块、固定种子和固定内容版本。
- PvP 不销售数值，不允许客户端决定命中、伤害、随机数、地形变化或胜负。
- AI 不得读取玩家尚未确认的操作，也不得绕过正常战斗指令直接修改状态。

## 2.4 确定性

以下系统必须确定性执行：

- 弹道。
- 碰撞。
- 地形破坏。
- 支撑计算。
- 局部坍塌。
- 状态效果。
- AI 随机误差。
- 奖励三选一。
- 路线生成。
- 每日挑战。
- 回放复算。
- PvP 服务端裁定。

同一规则版本、内容版本、初始种子和指令序列必须产生一致结果。

## 2.5 完整产品

V1.0 至少必须包含：

- 6 个教学任务。
- 完整标准远征。
- 4 个区域。
- 4 个机制 Boss。
- 6 名可解锁机器人。
- 18 个工程模块及双升级路线。
- 8 个基础敌人原型及精英变体。
- 关卡、事件、工坊、解锁、图鉴、成就、设置。
- 节点级本地与云端存档。
- 每日固定种子挑战。
- 每日三次正式机会及无限练习。
- 匿名全球排行榜。
- 用户协议、隐私政策入口、账户注销和数据删除流程。
- 可视化内容编辑器。
- 管理后台。
- 自动化测试、部署配置、监控、备份和恢复文档。
- 正式的发布验收清单。

---

# 3. 核心产品定义

## 3.1 一句话描述

一款以“破坏、建造、修复和操控地形”为核心的 2D 手绘回合制工程战术肉鸽微信小游戏。玩家从 6 名迷你工程机器人中选择 3 名组成小队，在随机生成的浮空岛路线中完成救援、护送、修复、坚守和战斗任务。

## 3.2 世界观

天空世界由依靠能源核心维持漂浮的岛屿组成。一次“失重潮汐”导致中央维护网络崩溃，浮岛逐步坍塌。玩家率领独立工程机器人小队，修复能源设施、救援失控单位、重建道路并调查灾难原因。

基调：

- 轻松、可爱、略带轻废土机械感。
- 不使用拟真战争、血液、肢解或真实军事武器。
- 破坏表现以能量、烟尘、机械零件和地形碎屑为主。
- 机器人失效称为“失去动力”或“进入回收状态”，不使用死亡肢解表现。

## 3.3 核心体验

玩家每轮需要权衡：

- 攻击还是修复。
- 救援还是抢占能源。
- 炸开道路还是搭桥。
- 是否牺牲地形完整度换取短期收益。
- 三名机器人本轮的行动顺序。
- 共享能源如何分配。
- 哪些模块应为后续节点保留。
- 如何同时完成主要目标、次要目标和隐藏工程目标。

## 3.4 非目标

V1.0 不做：

- 商业化。
- 自由社交。
- 自定义昵称。
- 好友关系读取。
- 配音。
- 剧情动画。
- 完整柔体或粒子沙盒物理。
- 完全随机、未经验证的地图。
- 大量纯数值养成。
- 玩家间交易。
- UGC 公开发布。
- 实时 PvP；实时 PvP 在 V2.0 实现。

---

# 4. 核心玩法规则

## 4.1 单局结构

标准远征：

- 目标时长：熟练玩家 35～45 分钟；首次游玩允许 45～55 分钟。
- 4 个区域。
- 每个区域生成 4～6 个可选节点。
- 玩家实际经过 2 个普通/非战斗节点，再进入区域 Boss。
- 完整一局约 12 个实际节点。
- 完成每个节点后自动保存。
- 失败后本局结束，结算少量研究资料。

节点类型：

- 普通战斗。
- 工程任务。
- 精英任务。
- 随机事件。
- 工坊。
- 补给。
- Boss。

路线可见性：

- 显示当前可选节点。
- 显示下一层节点类型和风险。
- 更远节点只显示连接和未知标记。
- 情报能力可以揭示更远信息。

## 4.2 小队

- 首发 6 名机器人。
- 每局选择 3 名。
- 每名机器人拥有：
  - 1 个固有被动。
  - 1 个专属技能。
  - 1 个主模块槽。
  - 2 个辅助模块槽。
- 专属技能不占模块槽。
- 角色强弱体现为适用场景不同，不允许后解锁角色全面碾压初始角色。

工作代号与定位：

| ID | 工作名称 | 定位 | 被动方向 | 专属技能方向 |
|---|---|---|---|---|
| `robot_rivet` | 铆钉 | 建造与修复 | 建筑耐久、修复效率 | 快速生成临时支架 |
| `robot_anchor` | 锚点 | 牵引与控制 | 磁力范围和精度 | 将目标拉向指定锚点 |
| `robot_gale` | 风栖 | 位移与救援 | 移动、跳跃效率 | 短距离喷射并携带友方 |
| `robot_prism` | 棱镜 | 弹道与侦察 | 轨迹和结构信息 | 扫描敌人、地形和隐藏装置 |
| `robot_forge` | 熔芯 | 破拆与开路 | 硬质材料破坏效率 | 定向钻爆 |
| `robot_echo` | 回声 | 能源与协同 | 辅助模块能源返还 | 复制上一名队员的辅助效果 |

名称均为工作代号，正式名称必须经过原创性和商标审查。

## 4.3 回合规则

PvE 使用整轮行动制：

1. 玩家规划阶段。
2. 玩家行动阶段。
3. 敌方行动阶段。
4. 环境结算阶段。

玩家行动阶段：

- 三名机器人各行动一次。
- 玩家自行决定行动顺序。
- 每名机器人通常可以移动一次。
- 每名机器人可以使用一个主要模块。
- 部分辅助动作不结束行动，必须由模块配置明确声明。
- 使用主要模块后，该机器人本轮行动结束。
- 可选择待机。
- 任务关键交互不得因能源不足而变成无解。

## 4.4 瞄准与输入

仅横屏。

支持两种显示方式，但底层使用同一指令模型：

1. 默认拖动瞄准：
   - 方向决定角度。
   - 距离决定力度。
   - 默认进入待确认状态，不直接松手发射。
2. 精确模式：
   - 显示角度。
   - 显示力度百分比。
   - 支持按钮微调。
   - 支持力度滑杆。
   - 显示风力和局部重力。

轨迹：

- 默认只显示前半段预测轨迹。
- 棱镜、扫描器或特殊规则可以延长显示。
- 反射模块只显示首个碰撞点，除非升级明确允许更多信息。

统一指令示例：

```ts
export interface UseModuleCommand {
  commandId: string;
  battleId: string;
  turnIndex: number;
  actorId: string;
  moduleId: string;
  originX: Fixed;
  originY: Fixed;
  angleMilliDegrees: number;
  powerPermille: number;
  targetX?: Fixed;
  targetY?: Fixed;
}
```

禁止客户端把浮点屏幕坐标直接作为权威战斗参数。

## 4.5 镜头

镜头模式：

```ts
export enum CameraMode {
  Overview = "overview",
  FollowActor = "follow_actor",
  ManualInspect = "manual_inspect",
  Aiming = "aiming",
  ProjectileFollow = "projectile_follow",
  ObjectiveFocus = "objective_focus",
  BossMechanicFocus = "boss_mechanic_focus",
}
```

要求：

- 自动跟随当前单位。
- 单指拖动空白区域查看地图。
- 双指缩放和平移。
- 瞄准手柄拖动优先于镜头拖动。
- 玩家手动查看后，短时间内不得被自动镜头强制抢回。
- 投射物和重大机制可以申请镜头控制，但由统一 `CameraDirector` 仲裁。
- UI 不随战场缩放。
- 提供减少镜头移动和减少震动选项。

---

# 5. 生命、结构损伤与恢复

## 5.1 双耐久

每名机器人拥有：

- `hp`：生命/动力耐久。
- `structuralDamage`：结构损伤，范围 0～100。

结构损伤阈值：

| 范围 | 状态 |
|---:|---|
| 0～29 | 正常 |
| 30～59 | 轻度故障 |
| 60～100 | 重度故障 |

故障来源应与受损类型相关：

- 坠落：移动与稳定故障。
- 能源过载：模块冷却或能源消耗故障。
- 强碰撞：击退与支撑故障。
- 磁场：瞄准或辅助模块故障。

限制：

- 每名机器人最多同时存在两个结构故障。
- 关键能力不得完全永久失效。
- 允许范围缩短、冷却增加、消耗增加或使用代价上升。
- 不能产生使任务失去唯一解法的故障。

## 5.2 失效

- `hp <= 0` 时退出当前战斗。
- 留下回收信标。
- 不再参与本场行动顺序。
- 任务物品掉落到经过确定性安全校验的位置。
- 节点胜利后重新加入并恢复部分生命。
- 节点结束不会完全清除结构损伤。
- 三名机器人全部失效时，远征失败。

默认恢复比例必须配置化，推荐初值：

- 存活机器人：恢复最大生命的 15%。
- 失效机器人：恢复至最大生命的 35%。
- 结构损伤仅部分降低。
- 工坊是主要深度修复渠道。

---

# 6. 能源与模块

## 6.1 小队共享能源

推荐初值，必须配置化：

- 能源上限：12。
- 每轮恢复：6。
- 未使用能源可保留。
- 普通移动不消耗小队能源。
- 主模块消耗 2～5。
- 辅助模块消耗 1～3。
- 强力模块同时具有冷却。
- 待机可以提供小幅能源收益，但不得形成无限拖延最优策略。

必须保留的兜底：

- 能源不足仍可移动。
- 基础推击或基础维修能力不得被完全锁死。
- 任务关键交互不消耗能源。
- 内容校验器必须拒绝理论能源上限无法完成的任务。

## 6.2 首发 18 个模块

### 主模块 8 个

| ID | 工作名称 | 核心能力 |
|---|---|---|
| `main_fold_bridge` | 折叠桥 | 在合法支点间生成临时桥梁 |
| `main_drill_bee` | 定向钻蜂 | 沿指定方向钻入并局部破坏地形 |
| `main_magnetic_anchor` | 磁力锚 | 拉动金属物、单位或任务目标 |
| `main_gravity_pin` | 重力钉 | 临时改变局部重力方向 |
| `main_bubble_capsule` | 泡泡胶囊 | 包裹单位或形成弹性碰撞体 |
| `main_wind_generator` | 风场发生器 | 创建持续气流 |
| `main_support_frame` | 快速支撑架 | 为不稳定地形提供临时支撑 |
| `main_energy_rail` | 能源导轨 | 改变并加速经过的投射物 |

### 辅助模块 10 个

| ID | 工作名称 | 核心能力 |
|---|---|---|
| `aux_repair_spray` | 修复喷雾 | 修复单位或地形 |
| `aux_reflector` | 反射护板 | 改变首次命中的投射物方向 |
| `aux_ejector` | 紧急弹射舱 | 快速转移自己或相邻友方 |
| `aux_stabilizer` | 稳定脚架 | 降低击退与坠落风险 |
| `aux_route_scanner` | 路线扫描器 | 揭示路线、隐藏目标或延长轨迹 |
| `aux_energy_recycler` | 能源回收器 | 达成条件后返还能量 |
| `aux_grapple` | 微型牵引索 | 将自身拉向地形锚点 |
| `aux_terrain_foam` | 地形泡沫 | 临时填补小型缺口 |
| `aux_jammer` | 干扰信标 | 改变敌人目标选择评分 |
| `aux_structure_scanner` | 结构检测仪 | 显示支撑点与坍塌风险 |

## 6.3 升级路线

每个模块至少有两条互斥路线。升级必须改变玩法，不得只做线性伤害叠加。

示例：

- 折叠桥：
  - 加固路线。
  - 导能路线。
- 磁力锚：
  - 强牵引路线。
  - 精密控制路线。
- 定向钻蜂：
  - 深层钻进路线。
  - 精密工程路线。

每条路线至少包含：

- 1 个机制变化。
- 1 个使用条件或策略变化。
- 允许附带少量数值变化。
- 完整的兼容性和非法组合校验。

---

# 7. 地形系统

## 7.1 基本实现

采用：

- 逻辑栅格。
- Chunk 分区。
- 材质属性。
- 固定锚点。
- 局部支撑连通性分析。
- 确定性局部坍塌。
- 渲染轮廓与逻辑栅格分离。

禁止：

- 把 Cocos 物理引擎结果直接作为权威判定。
- 完整柔体。
- 长时间存活的海量碎屑刚体。
- 依赖帧率的坍塌。
- 全图每帧重算。

推荐初值：

- Chunk：32×32 逻辑单元。
- 地形修改只标记脏 Chunk。
- 碰撞轮廓通过确定性轮廓生成算法更新。
- 碎屑表现不参与长期权威模拟。
- 大型无支撑区域合并为有限数量的坍塌块。

## 7.2 四种基础材质

| ID | 工作名称 | 硬度 | 支撑 | 特性 |
|---|---|---:|---:|---|
| `terrain_cloud_soil` | 云壤复合层 | 低 | 中 | 易挖掘、易修复 |
| `terrain_alloy_frame` | 合金骨架 | 高 | 高 | 可受磁力影响 |
| `terrain_energy_crystal` | 能源晶体 | 中低 | 低 | 破坏后释放能源或冲击 |
| `terrain_elastic_moss` | 弹性云苔 | 低 | 低 | 缓冲坠落、反弹投射物 |

材质必须影响：

- 破坏效率。
- 修复成本。
- 支撑能力。
- 弹道碰撞。
- 特殊模块互动。
- 视觉纹理和无障碍图案。

## 7.3 支撑与坍塌

流程：

1. 地形发生修改。
2. 标记受影响 Chunk。
3. 重新计算局部连通组件。
4. 检查组件是否连接固定锚点或有效支撑结构。
5. 无支撑组件进入坍塌队列。
6. 使用确定性轨迹结算坠落、碰撞、伤害和新位置。
7. 生成表现事件。
8. 更新状态哈希。

地图在进入战斗前必须通过：

- 出生点不重叠。
- 目标不直接坠落。
- 至少存在一种合法完成路径。
- AI 能进入关键交战区。
- 地形不会首帧整体坍塌。
- 任务不依赖玩家未拥有且关卡未提供的能力。
- 摄像机边界覆盖所有关键区域。

---

# 8. 地图与路线生成

采用“手工模板 + 程序变体”。

人工模板定义：

- 地形骨架。
- 固定锚点。
- 出生区域。
- 敌人候选区域。
- 目标候选区域。
- 机关槽位。
- 建造连接点。
- 危险区。
- 摄像机边界。
- 导航辅助标记。
- 任务关键通路。
- 合法材质分区。

程序可改变：

- 局部地形形状。
- 小型浮岛高度。
- 地形材质。
- 薄弱点。
- 具体出生点。
- 敌人组合。
- 环境参数。
- 次要目标。
- 隐藏目标。
- 装饰物。
- 机关类型。

生成器必须：

- 使用固定种子。
- 使用独立 RNG 子流。
- 失败时只重新生成失败部分。
- 输出可读的验证报告。
- 支持从种子一键重现。
- 支持在内容编辑器中预览和批量模拟。

---

# 9. 任务目标

每个战斗节点最多包含：

1. 主要目标：必须完成。
2. 次要目标：增加奖励。
3. 隐藏工程目标：鼓励特殊解法。

主要目标候选：

- 修复能源塔。
- 运送能源核心。
- 救援单位。
- 坚守若干轮。
- 击败守卫。
- 防止浮岛崩塌。
- 关闭污染节点。

次要目标候选：

- 无机器人失效。
- 地形完整度达到阈值。
- 限定轮数内完成。
- 不使用某类模块。
- 额外救援。
- 清理污染源。

隐藏目标候选：

- 利用磁力使敌人互撞。
- 无直接攻击完成救援。
- 修复隐藏能源管道。
- 经两次反射命中目标。
- 保留古老机械遗迹。

隐藏目标奖励：

- 模块蓝图。
- 世界观档案。
- 外观。
- 特殊挑战。
- 图鉴。

---

# 10. 肉鸽成长与奖励

## 10.1 局外成长

只允许解锁：

- 机器人。
- 模块。
- 升级路线。
- 新区域。
- 事件。
- 关卡模板。
- 高难度规则。
- 图鉴。
- 外观。
- 挑战模式。

禁止永久增加：

- 基础生命。
- 基础伤害。
- 初始能源。
- 命中率。
- 暴击率。
- 固定减伤。
- 奖励倍率。

## 10.2 失败

- 远征失败后本局临时构筑清空。
- 保留少量研究资料。
- 无节点完成则不结算研究资料。
- 重复快速失败奖励衰减。
- 首次发现、首次完成和隐藏目标是主要研究资料来源。
- 所有结算公式配置化。

## 10.3 三选一

战斗或精英节点后，从合法池中生成三个选项：

- 新模块。
- 模块升级。
- 当前远征临时改造。
- 一次性物资。
- 情报。

约束：

- 至少一个选项与当前队伍兼容。
- 不生成无可用角色的专属升级。
- 三个选项不能功能完全同质。
- 不连续多次只给攻击类奖励。
- 已持有模块应提高对应升级出现概率。
- 稀有度优先体现机制复杂度，不只体现数值。
- 奖励结果在首次生成时锁定，退出重进不能刷新。

---

# 11. 敌人与 AI

## 11.1 8 个基础原型

| ID | 工作名称 | 作用 |
|---|---|---|
| `enemy_scout` | 巡线侦察机 | 标记玩家，提高友军瞄准 |
| `enemy_guard` | 铆盾守卫机 | 展开护板，保护关键目标 |
| `enemy_artillery` | 抛投作业机 | 远距离弹道压力 |
| `enemy_driller` | 钻掘破拆机 | 破坏桥梁和支撑 |
| `enemy_magnet` | 磁力牵引机 | 改变单位和金属物位置 |
| `enemy_repairer` | 修补维护机 | 修复敌人、建筑和地形 |
| `enemy_wind` | 扰流控制机 | 生成风场，干扰弹道 |
| `enemy_carrier` | 搬运执行机 | 夺取任务物品 |

## 11.2 AI 架构

- 行为树负责宏观状态。
- 效用评分负责具体动作选择。
- AI 与玩家使用同一 `BattleCommand` 接口。
- AI 不得直接调用扣血、瞬移或破坏状态。
- AI 候选动作必须通过同一合法性检查。
- 所有随机误差来自种子 RNG。

效用评分至少考虑：

- 任务收益。
- 预计伤害。
- 地形收益。
- 自身安全。
- 控制收益。
- 能源成本。
- 误伤风险。
- 坠落风险。
- 下轮暴露风险。

## 11.3 难度

普通、困难、专家三级。

难度通过以下维度调整：

- AI 候选位置数量。
- 目标权重。
- 风力修正能力。
- 角度与力度误差。
- 精英词缀。
- 环境组合。
- 工坊数量。
- 结构损伤恢复。
- 路线信息。
- Boss 机制。

禁止：

- 统一把敌人生命乘以极大倍率。
- AI 读取未确认输入。
- AI 绕过弹道。
- 专家难度生成无解地图。
- 暗中修改玩家命中结果。

## 11.4 精英词缀

首发可选：

- 稳定核心。
- 反射外壳。
- 过载回路。
- 协同协议。
- 紧急修补。
- 悬浮底盘。

词缀组合使用白名单，不允许自动生成未经测试的任意组合。

---

# 12. Boss

首发 4 个多阶段机制 Boss。

## 12.1 裂岩钻机

- 持续切断支撑。
- 必须建造临时结构。
- 先破坏外部钻头，再攻击核心。
- 强行输出可能导致战场坍塌。

## 12.2 极冠磁塔

- 周期性改变金属物吸引方向。
- 搬运能源核心。
- 关闭多个磁场节点。
- 合金地形重排。

## 12.3 倒悬控制器

- 局部重力周期翻转。
- 修复稳定装置。
- 利用反向重力攻击弱点。

## 12.4 失控浮岛主机

- 同时影响地形、能源和敌人生成。
- 需要在多个子目标之间分配行动。
- 最终阶段通过修复、关闭节点和核心攻击组合完成。

当前不制作 Boss 剧情动画。阶段切换使用：

- UI 警告。
- 镜头聚焦。
- 地形变化。
- 音效。
- 简短文本。
- 程序化表现。

---

# 13. 教学

6 个独立短教学，每关 2～4 分钟：

1. 镜头、缩放、选择和移动。
2. 弹道、力度、风力和确认发射。
3. 地形材质、破拆、支撑和坍塌。
4. 建造、修复和合法性预览。
5. 三机器人行动顺序与共享能源。
6. 精简远征：路线、战斗、事件、奖励和结算。

要求：

- 文本可跳过。
- 关键操作不可跳过，必须验证。
- 每个教学只引入有限新概念。
- 完成教学 6 后开放标准远征。
- 每日挑战练习可开放，但正式计分应在玩家完成标准远征第一区域后开放。

---

# 14. 中断、重开与存档

## 14.1 存档类型

### 账户进度

- 解锁。
- 图鉴。
- 成就。
- 外观。
- 教学。
- 设置。
- 统计。

### 当前远征

- 种子。
- 区域和节点。
- 队伍状态。
- 模块和升级。
- 资源。
- 路线。
- 事件。
- Boss 状态。
- 内容版本。
- 规则版本。

### 战斗恢复快照

每轮开始保存：

- 单位状态。
- 地形。
- 支撑关系。
- 目标进度。
- 能源。
- 冷却。
- RNG 状态。
- 环境状态。
- 当前轮。
- 已触发事件。
- 版本。
- 状态哈希。

## 14.2 中断恢复

- 恢复至当前轮开始。
- RNG 状态保持。
- 恢复后相同指令必须产生相同结果。
- 快照损坏时回退至节点开始。
- 版本不兼容时执行迁移；无法迁移则安全回退，不直接删除整局。

## 14.3 主动重开

- 每个节点最多一次。
- 返回节点初始状态。
- 地图、敌人、事件、奖励和隐藏目标不变。
- 使用后写入存档。
- 清缓存或切设备不能恢复次数。
- 每日正式挑战中重开不返还正式机会。

---

# 15. 每日挑战与匿名排行榜

## 15.1 挑战规则

每天固定：

- 三名机器人。
- 主模块和辅助模块。
- 初始能源和耐久。
- 路线。
- 地图变体。
- 敌人。
- 事件。
- 奖励三选一。
- 难度。
- 内容版本。
- 规则版本。

挑战 ID 至少包含：

```text
server_date + rules_version + content_version + challenge_seed
```

不得信任客户端日期。

## 15.2 机会

- 无限练习，不计分。
- 每天三次正式机会。
- 正式开始时服务端原子扣减一次。
- 主动放弃消耗机会。
- 网络中断允许恢复。
- 客户端崩溃允许从合法快照恢复。
- 重复提交必须幂等。

## 15.3 评分

推荐初始权重：

- 主要任务完成：40%。
- 次要与隐藏目标：20%。
- 剩余状态：15%。
- 地形完整度：10%。
- 行动轮数：10%。
- 能源效率：5%。

服务端重新计算最终得分。

## 15.4 排行榜

公开展示：

- 排名。
- 系统代号。
- 原创机器人头像。
- 总分。
- 完成时间。
- 使用轮数。
- 完成状态。

不展示：

- 微信昵称。
- 微信头像。
- OpenID。
- UnionID。
- 手机号。
- 地区。
- 设备型号。
- 精确在线时间。

系统代号由服务端词库生成，不允许自由文本。

## 15.5 成绩验证

客户端上传：

- 初始种子。
- 内容版本。
- 规则版本。
- 指令序列。
- 节点哈希。
- 最终状态摘要。
- 恢复次数。
- 客户端版本。

服务端或验证 Worker：

1. 加载相同版本。
2. 复算战斗。
3. 比较状态哈希。
4. 重新计算分数。
5. 验证通过后写榜。
6. 验证失败不公开，并记录匿名风险事件。

---

# 16. 无障碍与易用性

必须从第一版 UI 架构中实现：

## 16.1 视觉

- 色觉辅助预设。
- 阵营同时使用颜色、轮廓和图标。
- 材质使用纹理图案。
- 建造合法性使用颜色、图案和文字。
- 可调文字大小。
- 高对比模式。
- 降低闪光。
- 减少镜头震动。
- 减少快速镜头运动。
- 重要状态不能只依赖颜色。

## 16.2 操作

- 左手/右手 UI。
- 瞄准灵敏度。
- 镜头灵敏度。
- 可关闭松手发射。
- 长按或二次确认防误触。
- 触控热区大于视觉图标。
- 可随时查看任务说明。
- 角度和力度精确微调。

## 16.3 音频

所有关键音频提示必须有视觉或文字替代。

设置：

- 主音量。
- 音乐。
- 环境。
- 战斗音效。
- UI 音效。
- 震动。
- 后台自动暂停或降音量。

设置本地立即生效并云端同步。

---

# 17. 美术、音频与动画边界

## 17.1 视觉方向

- 2D 手绘卡通。
- 圆润工程机器人。
- 明亮天空。
- 低饱和机械遗迹。
- 轻微材质笔触。
- 区域拥有不同植物、材料和环境语言。
- 不使用真实武器外形。
- 不使用虫状角色。

## 17.2 当前动画

暂不制作：

- 剧情过场。
- 视频 CG。
- 配音。
- 口型。
- 大量角色走位演出。
- Boss 长登场动画。

必须实现：

- 待机反馈。
- 移动反馈。
- 瞄准和蓄力。
- 模块发射和生效。
- 受击和击退。
- 失效和回收。
- 地形破坏、修复、坍塌。
- UI 点击、奖励、结算。
- Boss 阶段提示。
- 镜头运动。
- 状态循环提示。

开发阶段可以使用原创程序化图形和占位切片，但：

- 占位资源必须明显标记。
- 发布验收不得残留占位素材。
- Codex 不得声称占位资源达到正式美术完成度。

## 17.3 音频

首发：

- 完整 UI 和战斗音效。
- 少量通用背景音乐。
- 无配音。

建议音乐：

- 主界面。
- 普通探索。
- 高危/精英。
- Boss。
- 结算/工坊。

所有素材必须原创或有明确商用授权。

---

# 18. 仓库结构

```text
project-skyforge/
├─ AGENTS.md
├─ README.md
├─ package.json
├─ pnpm-workspace.yaml
├─ turbo.json
├─ docker-compose.yml
├─ .env.example
├─ .editorconfig
├─ .gitattributes
├─ .gitignore
│
├─ apps/
│  ├─ game-client/              # Cocos Creator 3.8 LTS 微信小游戏
│  ├─ game-server/              # NestJS 模块化单体 API
│  ├─ game-worker/              # 回放复算、内容验证、聚合任务
│  ├─ content-studio/           # React 可视化内容编辑器
│  ├─ content-gateway/          # 仅本地默认开放的内容文件服务
│  └─ admin-console/            # React 管理后台
│
├─ packages/
│  ├─ battle-core/
│  ├─ terrain-core/
│  ├─ ai-core/
│  ├─ deterministic-runtime/
│  ├─ content-schema/
│  ├─ content-runtime/
│  ├─ protocol/
│  ├─ shared-types/
│  ├─ save-migration/
│  ├─ localization/
│  ├─ security/
│  ├─ telemetry/
│  └─ test-fixtures/
│
├─ content/
│  ├─ robots/
│  ├─ modules/
│  ├─ enemies/
│  ├─ bosses/
│  ├─ maps/
│  ├─ objectives/
│  ├─ events/
│  ├─ routes/
│  ├─ challenges/
│  ├─ tutorials/
│  ├─ localization/
│  └─ manifests/
│
├─ assets/
│  ├─ development/
│  ├─ release/
│  └─ provenance/
│
├─ tools/
│  ├─ content-validator/
│  ├─ replay-runner/
│  ├─ balance-simulator/
│  ├─ asset-auditor/
│  ├─ package-budget/
│  ├─ cocos-build/
│  └─ database/
│
├─ infra/
│  ├─ docker/
│  ├─ compose/
│  ├─ observability/
│  ├─ backup/
│  └─ k8s-future/
│
├─ docs/
│  ├─ adr/
│  ├─ architecture/
│  ├─ design/
│  ├─ legal/
│  ├─ operations/
│  ├─ publishing/
│  ├─ security/
│  ├─ status/
│  ├─ testing/
│  └─ ux/
│
└─ .github/
   ├─ workflows/
   ├─ ISSUE_TEMPLATE/
   └─ pull_request_template.md
```

---

# 19. 技术栈与版本策略

## 19.1 Node

- 使用 Node.js 24 LTS。
- `.nvmrc`、`.node-version`、`package.json#engines` 和 CI 保持一致。
- 不使用 Current 或 EOL 版本作为生产基线。
- 依赖升级必须单独 PR，并通过确定性回放测试。

## 19.2 TypeScript

- `strict: true`。
- 禁止隐式 `any`。
- 公共包必须显式导出类型。
- 业务枚举优先使用字符串联合或 `as const`，避免运行时不必要枚举。
- 所有跨应用 DTO 由 `packages/protocol` 统一定义。
- 运行时输入必须验证，不能只依赖 TypeScript 类型。

## 19.3 包管理

- pnpm。
- 锁文件必须提交。
- 禁止混用 npm/yarn lockfile。
- Turborepo 管理构建、测试、类型检查和缓存。

## 19.4 服务端

- NestJS 11。
- 默认 Fastify Adapter。
- REST + JSON。
- OpenAPI 生成并校验。
- Prisma ORM 优先；若出现明确兼容问题，可通过 ADR 改用其他成熟 ORM。
- Redis 使用稳定、维护活跃的客户端。
- BullMQ 或等价队列用于异步验证任务。
- 禁止为了“微服务化”提前拆分大量独立服务。

## 19.5 Web 工具

- React。
- Vite。
- TypeScript。
- React Router。
- 成熟的表单和状态方案，但不得引入不必要的大型框架。
- Playwright 进行 E2E。
- 内容编辑器默认通过仅监听 localhost 的 `content-gateway` 访问仓库文件。

## 19.6 Cocos

- Cocos Creator 3.8 LTS。
- 使用 TypeScript。
- 引擎仅负责渲染、输入、音频、资源和表现。
- `battle-core`、`terrain-core` 不依赖 Cocos API。
- 微信构建由 `tools/cocos-build` 包装。
- Cocos 可执行路径通过 `COCOS_CREATOR_PATH` 配置。
- 使用 Asset Bundle 和分包策略。
- 是否启用微信小游戏引擎插件必须经过真机兼容和包体测试。

---

# 20. 确定性运行时

## 20.1 数值

- 权威逻辑禁止使用未经封装的浮点计算。
- 坐标、速度、角度和力度使用固定点整数。
- 推荐 `FIXED_SCALE = 1000`，最终值由 ADR 固化。
- 所有乘除使用安全整数检查。
- 坐标范围必须避免超过 JavaScript 安全整数。
- 三角函数使用预生成整数查表。
- 禁止在权威逻辑中使用：
  - `Math.random()`
  - `Date.now()`
  - 设备帧时间
  - Cocos 物理结果
  - 不稳定对象遍历顺序

## 20.2 RNG

- 使用明确实现的 32 位确定性 RNG。
- 推荐 `xoshiro128**` 或经测试的等价算法。
- 通过根种子派生独立子流：
  - 地图。
  - 敌人。
  - AI。
  - 命中误差。
  - 奖励。
  - 故障。
  - 事件。
  - 隐藏目标。
- 一个子系统新增随机调用不能改变其他子系统结果。

## 20.3 状态哈希

- 核心状态采用规范化序列化。
- 逻辑层使用跨运行时一致的确定性哈希。
- 服务端提交使用加密哈希或签名保护。
- 回放文件包含：
  - 规则版本。
  - 内容版本。
  - 初始种子。
  - 指令。
  - 周期性状态哈希。
  - 最终摘要。

## 20.4 版本

必须独立维护：

- `clientVersion`
- `serverVersion`
- `rulesVersion`
- `contentVersion`
- `saveSchemaVersion`
- `replaySchemaVersion`
- `protocolVersion`

不得用单一 `appVersion` 替代全部兼容性判断。

---

# 21. 客户端架构

推荐模块：

```text
game-client/assets/scripts/
├─ bootstrap/
├─ platform/
│  ├─ wechat/
│  └─ mock/
├─ battle/
│  ├─ presentation/
│  ├─ input/
│  ├─ camera/
│  ├─ ui/
│  └─ effects/
├─ expedition/
├─ tutorial/
├─ profile/
├─ challenge/
├─ leaderboard/
├─ settings/
├─ accessibility/
├─ audio/
├─ content/
├─ save/
├─ network/
└─ diagnostics/
```

规则：

- 场景脚本不直接修改战斗状态。
- 表现层订阅战斗事件。
- 网络层不能把平台 DTO 泄漏到领域层。
- 微信 API 通过 `PlatformAdapter` 封装。
- 开发环境提供 Web/Mock 适配器。
- UI 状态与战斗状态分离。
- 所有异步页面具有加载、重试、离线和错误状态。
- 不使用真实用户昵称/头像作为默认 UI。

---

# 22. 服务端模块化单体

`apps/game-server` 推荐模块：

```text
src/modules/
├─ auth/
├─ accounts/
├─ profiles/
├─ unlocks/
├─ saves/
├─ expeditions/
├─ content/
├─ daily-challenges/
├─ attempts/
├─ replays/
├─ leaderboards/
├─ telemetry/
├─ privacy/
├─ admin/
├─ health/
├─ audit/
└─ platform-compliance/
```

要求：

- 模块间通过显式服务接口或领域事件交互。
- 数据库表不作为跨模块公共 API。
- 排行榜、回放验证和 PvP 未来可拆分，但当前不提前微服务化。
- 所有写接口支持幂等性。
- 所有管理员写操作进入审计日志。
- 所有 API 有 DTO 验证、授权、限流和统一错误码。
- 对外错误不得暴露堆栈、SQL、OpenID 或内部路径。

---

# 23. 微信登录与账户安全

流程：

1. 客户端调用平台登录 API 获取一次性 code。
2. 客户端只把 code 发送给自有服务端。
3. 服务端使用服务器环境中的平台凭证交换用户标识。
4. 服务端生成内部账户 ID。
5. 客户端获得短期访问令牌和可轮换刷新机制。
6. 原始平台密钥永不返回客户端。

存储：

- 优先保存 `HMAC-SHA256(openid, server_pepper)` 形式的稳定平台主体键。
- 仅在确有平台功能需求时，经过 ADR 和安全评审后保存加密原始标识。
- 不收集 UnionID，除非未来跨应用账户确有必要。
- 不把 OpenID 写入日志、分析事件、排行榜或错误响应。
- 刷新令牌仅保存哈希。
- 支持会话吊销。
- 支持账户注销和删除。

客户端不得：

- 内置 AppSecret。
- 信任客户端传来的 OpenID。
- 使用固定明文用户 ID 作为认证。
- 把平台 code 当作长期令牌。
- 在本地存储长期服务端密钥。

---

# 24. 数据模型

至少包含：

- `accounts`
- `platform_subjects`
- `sessions`
- `profiles`
- `profile_settings`
- `unlocks`
- `achievements`
- `expedition_saves`
- `battle_snapshots`
- `daily_challenges`
- `daily_attempts`
- `replay_submissions`
- `leaderboard_entries`
- `content_versions`
- `content_publications`
- `privacy_requests`
- `admin_users`
- `audit_logs`
- `telemetry_batches`
- `telemetry_daily_aggregates`

要求：

- 主键使用 UUIDv7 或有序等价方案。
- 所有时间由服务端 UTC 存储。
- 用户界面按用户时区显示；每日挑战按明确配置的服务端业务时区切换。
- 写入包含 `created_at`、`updated_at`。
- 需要软删除时明确说明；隐私删除不能只做软删除。
- 数据库迁移必须可回滚或提供前滚修复。
- 存档内容使用版本化 JSON/二进制结构，不得无版本直接存任意对象。
- 排行榜条目保留验证状态。

---

# 25. API 边界

至少实现：

## Auth

- `POST /v1/auth/wechat`
- `POST /v1/auth/refresh`
- `POST /v1/auth/logout`

## Profile

- `GET /v1/profile`
- `PATCH /v1/profile/settings`
- `GET /v1/profile/unlocks`
- `GET /v1/profile/achievements`

## Saves

- `GET /v1/saves/expedition`
- `PUT /v1/saves/expedition`
- `POST /v1/saves/resolve-conflict`
- `DELETE /v1/saves/expedition`

## Content

- `GET /v1/content/manifest`
- `GET /v1/content/versions/:version`

## Daily challenge

- `GET /v1/challenges/daily`
- `POST /v1/challenges/daily/practice/start`
- `POST /v1/challenges/daily/attempts/start`
- `PUT /v1/challenges/daily/attempts/:id/checkpoint`
- `POST /v1/challenges/daily/attempts/:id/finish`
- `POST /v1/challenges/daily/attempts/:id/abandon`

## Leaderboard

- `GET /v1/leaderboards/daily`
- `GET /v1/leaderboards/daily/me`

## Privacy

- `POST /v1/privacy/export`
- `POST /v1/privacy/delete-account`
- `GET /v1/privacy/requests/:id`

## Telemetry

- `POST /v1/telemetry/batches`

要求：

- OpenAPI 文档自动生成。
- 请求和响应 Schema 与 `packages/protocol` 同源。
- 使用 `Idempotency-Key` 保护关键写操作。
- 分页使用游标。
- 排行榜接口不返回任何平台标识。
- 错误码稳定且国际化展示由客户端处理。

---

# 26. 云存档冲突

离线优先：

- 本地先写。
- 网络恢复后同步。
- 每条存档包含版本、逻辑时钟、内容版本和摘要。
- 服务端拒绝明显回退或非法篡改。
- 普通账户进度尽可能按字段合并。
- 当前远征存档不能盲目合并两个分叉状态。

冲突 UI：

- 展示设备、时间、区域、节点和关键进度。
- 不显示内部 JSON。
- 玩家可选择本地或云端版本。
- 选择后生成新的权威版本。
- 保留短期恢复记录，设置合理过期时间。

---

# 27. 内容系统

## 27.1 Schema

- 使用 Zod 或等价运行时 Schema。
- 可生成 JSON Schema。
- 所有内容具有稳定 ID。
- 所有引用必须可静态验证。
- 禁止在内容中执行任意 JavaScript。
- 数值范围必须显式定义。
- 文本使用本地化 Key。
- 资源使用 Asset ID，不使用散落路径字符串。

## 27.2 内容版本

发布流程：

1. 编辑。
2. Schema 校验。
3. 引用校验。
4. 地图可解性校验。
5. 批量模拟。
6. 生成内容包。
7. 生成哈希。
8. 生成变更清单。
9. 测试环境发布。
10. 人工批准。
11. 签名。
12. 生产发布。
13. 可回滚。

每日挑战绑定内容版本，当天不得静默变更。

---

# 28. Skyforge Content Studio

React 浏览器应用，默认通过本地 `content-gateway` 访问仓库内容。

功能：

## 地图

- 栅格材质笔刷。
- 锚点。
- 出生区域。
- 敌人候选区域。
- 目标候选区域。
- 机关槽。
- 建造点。
- 危险区。
- 摄像机边界。
- 导航标记。
- 生成预览。
- 可解性检查。

## 路线

- 节点图。
- 连接。
- 权重。
- 层级。
- 出现次数限制。
- Boss 出口。
- 可达性检查。

## 机器人和模块

- 参数。
- 能源。
- 冷却。
- 目标类型。
- 状态。
- 升级路线。
- 资源引用。
- 兼容性。

## 敌人和 AI

- 行为树。
- 效用权重。
- 瞄准误差。
- 精英词缀。
- 区域变体。
- 合法组合。

## 事件和剧情

- 静态背景。
- 头像。
- 文本。
- 选项。
- 条件。
- 结果。
- 解锁。
- 本地化 Key。

## 每日挑战

- 固定阵容。
- 固定模块。
- 种子。
- 难度。
- 评分。
- 正式机会。

编辑器要求：

- 撤销/重做。
- 脏状态提示。
- 自动保存草稿。
- 明确发布流程。
- Schema 错误定位到具体字段。
- 导入/导出。
- 变更 diff。
- 不允许未经校验直接写生产内容。

---

# 29. 管理后台

仅供授权管理员使用。

功能：

- 内容版本查看和发布审批。
- 每日挑战预览和冻结。
- 排行榜条目验证状态。
- 异常成绩隔离。
- 用户系统代号重置。
- 账户注销请求处理。
- 服务健康状态。
- 版本兼容矩阵。
- 审计日志。
- 公告管理。
- 风险开关。

禁止：

- 默认展示原始平台身份。
- 允许管理员直接修改排行榜分数而不审计。
- 在浏览器保存长期管理员密钥。
- 共用普通玩家认证。
- 无二次确认执行高风险操作。

---

# 30. 匿名数据分析

自建最小事件系统，不接入广告追踪 SDK。

允许事件：

- 教学开始/完成。
- 节点开始/完成/失败。
- 模块选择。
- 机器人组合。
- Boss 阶段失败。
- 性能摘要。
- 崩溃摘要。
- 存档冲突。
- 无障碍选项启用情况的匿名聚合。

禁止事件：

- 微信昵称。
- 微信头像。
- OpenID。
- 精确位置。
- 通讯录。
- 聊天内容。
- 原始设备标识。
- 完整 IP 长期保存。
- 用户自由文本。
- 未经说明的第三方 SDK 数据。

要求：

- 事件字段白名单。
- 客户端批量上传。
- 服务端限流。
- 短期原始保留。
- 尽快聚合。
- 文档化保存期限。
- 支持删除请求。
- 分析事件不得用于跨应用跟踪。

---

# 31. 安全要求

## 31.1 秘密

- `.env` 不提交。
- 提供 `.env.example`。
- CI 使用 Secret Store。
- Docker 镜像不包含秘密。
- 客户端不包含服务端秘密。
- 启用 secret scanning。
- 提交前扫描高熵字符串和常见密钥格式。

## 31.2 API

- TLS。
- 访问令牌短期有效。
- 刷新令牌轮换。
- 登录、挑战和排行榜限流。
- 高风险接口幂等。
- 输入长度和嵌套深度限制。
- 统一 DTO 验证。
- 防止批量赋值。
- 管理后台独立授权。
- 审计日志防篡改。

## 31.3 回放与作弊

- 不信任客户端最终分数。
- 不信任客户端时间。
- 不信任客户端剩余挑战次数。
- 不信任客户端 RNG。
- 复算关键挑战。
- 状态哈希异常进入隔离。
- 不自动公开验证失败成绩。
- 作弊风控不公开用户个人信息。

## 31.4 供应链

- 锁定依赖版本。
- 自动依赖审计。
- 生成 SBOM。
- 检查许可证。
- 禁止 GPL/AGPL 等可能与发布目标冲突的依赖，除非经过明确法律评审。
- 第三方包必须活跃维护、用途明确、体积合理。

---

# 32. 知识产权与素材台账

必须建立：

```text
docs/legal/
├─ IP_ORIGINALITY_GUIDELINES.md
├─ COMPETITOR_DIFFERENTIATION.md
├─ TRADEMARK_REVIEW_CHECKLIST.md
├─ ASSET_PROVENANCE_POLICY.md
├─ THIRD_PARTY_LICENSES.md
├─ GENERATIVE_AI_ASSET_POLICY.md
└─ RELEASE_LEGAL_CHECKLIST.md
```

每个正式素材记录：

- Asset ID。
- 文件名。
- 作者/供应方。
- 创建日期。
- 原始源文件位置。
- 授权类型。
- 商用范围。
- 修改权限。
- 是否使用生成式 AI。
- AI 工具和生成过程说明。
- 参考素材来源。
- 审核人。
- 发布状态。

发布构建必须运行 `asset-auditor`：

- 缺失来源则失败。
- 禁止许可则失败。
- 未批准 AI 资源则失败。
- 竞品关键词或资源指纹命中则进入人工审查。
- 字体无授权则失败。
- 音频无授权则失败。

正式名称发布前：

- 检索中国商标。
- 检索微信小游戏重名和近似名。
- 检索主要搜索引擎、应用商店和游戏数据库。
- 记录查询日期、类别和结果。
- 由专业人员确认风险。

---

# 33. 隐私与监管发布门

发布前必须核验：

- 个人信息保护法。
- 网络安全与数据安全要求。
- 微信小游戏当前隐私指引。
- 用户协议和隐私政策。
- 账户注销。
- 数据访问、更正、删除和导出流程。
- 第三方 SDK 清单。
- 网络游戏实名和防沉迷要求。
- 适龄提示。
- 网络游戏审批、出版和备案要求。
- 运营主体资质。
- 服务器和数据所在地。
- 客服与投诉渠道。
- 应急响应流程。

原则：

- 不因“免费、无内购”假设免除网络游戏审批或防沉迷义务。
- 不由项目自行收集身份证号，优先接入依法合规的平台或主管系统能力。
- 若平台要求实名或防沉迷，使用 `platform-compliance` 适配层，并把敏感数据隔离。
- 未完成法律和平台核验前，不得提交正式发布。

---

# 34. 性能与包体

## 34.1 客户端

目标：

- 中高端设备 60 FPS。
- 支持范围内低端设备稳定 30 FPS。
- 权威模拟与渲染帧率解耦。
- 不出现超过 100 ms 的常规主线程长任务。
- 大规模地形变化分预算处理。
- 音效使用对象池。
- 粒子和碎屑有上限。
- 场景切换释放无用资源。
- 纹理使用图集和合理压缩。

## 34.2 启动

- 首包只包含启动、登录、主界面和基础公共资源。
- 区域、音乐、地图和外观使用 Asset Bundle/分包。
- 包体限制不硬编码陈旧数字；构建工具读取当前项目配置的官方上限并执行 Gate。
- 真机记录首屏时间、首次可交互时间和内存峰值。

## 34.3 服务端

- 普通 API p95 在合理网络条件下小于 300 ms。
- 排行榜读取使用 Redis 缓存。
- 数据库写入保持事务短小。
- 回放复算进入 Worker，不阻塞 API。
- 所有服务提供健康检查。
- 进行挑战开始高峰和排行榜读取压测。

---

# 35. 测试

## 35.1 单元测试

必须覆盖：

- 固定点数学。
- RNG。
- 弹道。
- 碰撞。
- 伤害。
- 状态。
- 能源。
- 冷却。
- 地形破坏。
- 支撑。
- 坍塌。
- AI 评分。
- 路线生成。
- 奖励生成。
- 存档迁移。
- 评分。

目标：

- `battle-core`、`terrain-core`、`deterministic-runtime` 语句覆盖率不低于 95%，分支不低于 90%。
- 其他核心包语句覆盖率不低于 85%，分支不低于 80%。
- 覆盖率不能替代行为验收。

## 35.2 属性测试

使用属性测试验证：

- 相同输入必得相同输出。
- 能量不为负。
- HP 和结构损伤不越界。
- 无支撑组件最终进入稳定状态。
- 地图关键目标可达。
- 奖励三选一满足兼容性。
- 重开不刷新种子。
- 存档往返不丢字段。
- 指令重放结果一致。

## 35.3 Golden Replay

仓库保存一组固定回放：

- 基础弹道。
- 多材质爆破。
- 大型坍塌。
- 磁力。
- 重力翻转。
- 风场。
- Boss 阶段。
- 每日挑战。
- AI 决策。

任何规则修改都必须显式更新 `rulesVersion`，不得静默改写 Golden 结果。

## 35.4 E2E

- 内容编辑器：Playwright。
- 管理后台：Playwright。
- API：集成测试。
- 数据库：真实 PostgreSQL Testcontainer。
- Redis：真实 Redis Testcontainer。
- Cocos：关键页面和流程冒烟测试，配合微信开发者工具及真机检查。
- 发布前执行真实设备矩阵。

## 35.5 性能测试

- 标准地图。
- 最大允许地图。
- 连续爆破。
- 最大坍塌。
- Boss 复杂场景。
- 低端设备。
- 弱网。
- 后台恢复。
- 内存压力。

## 35.6 安全测试

- 身份伪造。
- 重放攻击。
- 幂等性。
- 权限越权。
- 存档篡改。
- 排行榜伪造。
- 恶意超大请求。
- 注入。
- Secret 泄漏。
- 依赖漏洞。
- 删除请求完整性。

---

# 36. CI/CD

GitHub Actions 至少包含：

1. `quality.yml`
   - 格式。
   - lint。
   - typecheck。
   - 单元测试。
   - 覆盖率。
2. `determinism.yml`
   - Golden Replay。
   - 跨 Node 运行。
   - 状态哈希。
3. `content.yml`
   - Schema。
   - 引用。
   - 地图验证。
   - 本地化。
   - 资产台账。
4. `security.yml`
   - 依赖审计。
   - Secret 扫描。
   - SAST。
   - SBOM。
   - 许可证。
5. `web-e2e.yml`
   - Content Studio。
   - Admin Console。
6. `server-integration.yml`
   - PostgreSQL。
   - Redis。
   - API。
   - 回放 Worker。
7. `build.yml`
   - 服务端镜像。
   - Web 工具。
   - Cocos 构建准备检查。
   - 包体预算。
8. `release-candidate.yml`
   - 迁移检查。
   - 内容签名。
   - 发布清单。
   - 产物校验。

要求：

- 主分支禁止直接提交。
- PR 必须通过所有必需检查。
- 发布产物可追溯到 commit SHA。
- Docker 镜像使用不可变标签。
- 数据库迁移在应用发布前验证。
- 内容和代码可以独立回滚。
- 不自动提交微信审核。

---

# 37. 本地开发

预期命令：

```bash
pnpm install
pnpm infra:up
pnpm db:migrate
pnpm db:seed
pnpm dev
```

质量命令：

```bash
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
pnpm test:coverage
pnpm test:determinism
pnpm test:integration
pnpm test:e2e
pnpm content:validate
pnpm assets:audit
pnpm build
```

Cocos：

```bash
pnpm cocos:check
pnpm cocos:build:wechat
```

规则：

- Windows、Linux CI 均需支持核心包和服务端。
- Cocos 构建若需要 Windows/macOS Runner，应在文档中明确。
- `.env.example` 包含字段说明，不包含真实值。
- `docker compose` 提供 PostgreSQL、Redis、MinIO 开发替代、可选 Prometheus/Grafana。
- `README.md` 必须让新开发者在一份文档内启动开发环境。

---

# 38. 可观测性与运维

必须实现：

- 结构化日志。
- Request ID。
- Trace ID。
- 日志脱敏。
- API 指标。
- 数据库指标。
- Redis 指标。
- Worker 队列指标。
- 回放验证耗时。
- 挑战失败率。
- 内容版本分布。
- 客户端崩溃摘要。
- 健康检查。
- 就绪检查。
- 备份。
- 恢复演练。
- 告警文档。

禁止日志字段：

- OpenID。
- AppSecret。
- 访问令牌。
- 刷新令牌。
- 完整授权响应。
- 用户真实昵称。
- 精确 IP 长期记录。
- 客户端任意自由文本。

---

# 39. V1.0 分阶段实施

任何阶段都不能用低质量临时代码污染后续阶段。

## Phase 0：治理与基础设施

交付：

- Monorepo。
- 工具链。
- CI。
- Docker Compose。
- 基础文档。
- ADR 模板。
- 状态跟踪。
- 许可证和素材政策。

Gate：

- 安装、lint、typecheck、基础测试和构建全部通过。

## Phase 1：确定性运行时

交付：

- Fixed math。
- RNG 子流。
- 指令模型。
- 事件模型。
- 快照。
- 哈希。
- 回放。
- Golden 测试。

Gate：

- Node 多次重放一致。
- 随机子流隔离测试通过。

## Phase 2：地形核心

交付：

- Chunk 栅格。
- 四种材质。
- 破坏。
- 修复。
- 支撑。
- 坍塌。
- 地图验证。
- 性能基准。

Gate：

- 大型坍塌可重放。
- 无全图每帧重算。
- 属性测试通过。

## Phase 3：战斗规则

交付：

- 三机器人整轮行动。
- 移动。
- 能源。
- 冷却。
- 生命。
- 结构损伤。
- 任务。
- 18 个模块的完整逻辑。
- 双升级路线数据结构。

Gate：

- 模块无空实现。
- 关键组合自动测试。
- 无能源死锁关卡。

## Phase 4：AI、敌人和 Boss

交付：

- 行为树。
- 效用评分。
- 难度误差。
- 8 个原型。
- 精英词缀。
- 4 个 Boss。
- AI 调试可视化。

Gate：

- AI 只使用合法指令。
- Boss 可通过多种合理策略完成。
- 普通/困难/专家差异不是纯数值倍率。

## Phase 5：Cocos 客户端

交付：

- 启动。
- 微信适配。
- 主界面。
- 战斗呈现。
- 横屏输入。
- 镜头。
- 瞄准。
- UI。
- 音频。
- 基础程序化动画。
- 设置。
- 无障碍。

Gate：

- 核心流程真机运行。
- 30 FPS 低端基线。
- UI 不依赖颜色单一表达。

## Phase 6：远征与内容

交付：

- 4 区域。
- 路线生成。
- 节点类型。
- 三选一。
- 工坊。
- 事件。
- 研究资料。
- 解锁。
- 图鉴。
- 成就。
- 6 个教学。
- 轻剧情文本。

Gate：

- 一局完整 35～45 分钟目标可达。
- 无占位内容逻辑。
- 内容数量达到 V1.0 下限。

## Phase 7：账户与云存档

交付：

- 微信登录适配。
- 账户。
- 会话。
- 本地存档。
- 云存档。
- 冲突解决。
- 每轮恢复。
- 节点一次重开。
- 迁移。

Gate：

- 弱网和中断恢复测试通过。
- AppSecret 不进入客户端。
- 删除账户流程可执行。

## Phase 8：每日挑战与排行榜

交付：

- 固定每日种子。
- 练习。
- 三次正式机会。
- 指令上传。
- Worker 复算。
- 匿名排行榜。
- 风险隔离。

Gate：

- 篡改分数不能入榜。
- 客户端改时间不能刷新次数。
- 排行榜无平台个人信息。

## Phase 9：内容编辑器和后台

交付：

- Content Studio 全模块。
- Content Gateway。
- Admin Console。
- 内容发布。
- 审计。
- 回滚。
- 版本冻结。

Gate：

- 不手改 JSON 也能完成主要内容生产。
- 非法内容无法发布。
- 管理操作可追溯。

## Phase 10：正式内容、性能和发布准备

交付：

- 正式原创或已授权素材导入。
- 资产台账。
- 音频。
- 包体优化。
- 设备矩阵。
- 隐私文档。
- 运维。
- 备份恢复。
- 法律发布清单。

Gate：

- 发布包无占位素材。
- 无未授权素材。
- 所有 P0/P1 问题关闭。
- 合规外部条件确认。

## Phase 11：V1.0 候选版本

交付：

- Release Candidate。
- 完整回归。
- 内容冻结。
- 迁移演练。
- 回滚演练。
- 微信开发者工具和真机报告。
- 审核材料草案。

Gate：

- 满足本文件 V1.0 Definition of Done。
- 只有外部审批/主体凭证可以作为剩余阻塞项。

---

# 40. V2.0 实时 PvP

V1.0 Gate 通过后再开始，不允许为了 PvP 破坏 PvE 稳定性。

## 40.1 范围

- 1v1。
- 每人控制 3 名机器人。
- 好友房。
- 实时匹配。
- 排位。
- 掉线重连。
- 战斗回放。
- 服务端权威裁定。
- 反作弊。
- 赛季基础结构。
- 无数值付费。

## 40.2 回合

PvP 使用交替单单位行动，避免一方连续完成三次操作造成先手压制：

- 每轮双方各有 3 名未行动单位。
- 轮次开局方按轮次交替。
- 双方交替选择一名未行动单位执行。
- 每名单位每轮一次。
- 行动时间初值 25 秒，可配置。
- 超时执行安全待机。
- 服务端维护权威时钟。
- 指令在服务端校验后广播事件。

如实际测试证明该规则不能满足 5～8 分钟目标，必须通过 ADR 调整，但不得退回客户端权威。

## 40.3 架构

新增边界：

```text
apps/
├─ pvp-gateway/
├─ pvp-battle-worker/
└─ matchmaking-worker/
```

可以先在模块化单体中实现接口，但生产 PvP 战斗进程应可独立扩缩容。

要求：

- WebSocket。
- 房间状态。
- 断线重连。
- 快照。
- 服务端战斗内核。
- 消息序列号。
- 幂等。
- 超时。
- 心跳。
- 观战只读协议。
- 赛后复算。
- 防止客户端伪造回合、能源或 RNG。

## 40.4 匹配

至少考虑：

- 段位。
- 等待时间扩展。
- 网络区域。
- 规则版本。
- 内容版本。
- 机器人和模块合法性。
- 避免重复匹配策略。

## 40.5 PvP Gate

- 断线 60 秒内可恢复。
- 双客户端状态与服务端一致。
- 恶意重放和乱序消息无效。
- 低质量网络可完成一局。
- 服务器重启有明确处理。
- 不展示微信个人资料。
- 一局时长中位数接近 5～8 分钟。
- 机器人和模块无局外数值优势。

---

# 41. 内容规模下限

V1.0 不低于：

| 内容 | 数量 |
|---|---:|
| 机器人 | 6 |
| 上阵机器人 | 3 |
| 工程模块 | 18 |
| 每模块升级路线 | 2 |
| 区域 | 4 |
| Boss | 4 |
| 普通战斗模板 | 24 |
| 工程任务模板 | 12 |
| 精英模板 | 8 |
| 随机事件 | 30 |
| 工坊事件 | 8 |
| 环境机制 | 8 |
| 地形材质 | 4 |
| 隐藏目标 | 20 |
| 可解锁外观 | 12 |
| 教学 | 6 |
| 剧情动画 | 0 |
| 自由聊天 | 0 |
| 广告 | 0 |
| 付费 | 0 |

内容数量达到不代表质量达标；重复换皮不计作独立内容。

---

# 42. Definition of Done

## 42.1 工程完成

- 所有必需应用可以构建。
- 所有核心包测试通过。
- CI 全绿。
- 没有核心路径空实现。
- 没有未跟踪 P0/P1 TODO。
- 数据库可从空库迁移。
- 备份可恢复。
- 内容可回滚。
- 版本兼容矩阵完整。
- 文档能指导新开发者启动。

## 42.2 产品完成

- 新用户可以登录、完成教学、开始远征、退出恢复、完成整局、解锁内容、参加每日挑战、查看匿名榜。
- 所有 6 名机器人和 18 个模块可用。
- 所有 4 个 Boss 可完成。
- 普通、困难、专家均可玩。
- 每日挑战固定且可验证。
- 无障碍功能可用。
- 音效完整。
- 当前范围内无剧情动画缺口，因为剧情动画不属于本阶段范围。

## 42.3 质量完成

- 无已知数据丢失。
- 无可复现状态不同步。
- 无客户端伪造成绩入榜。
- 无 AppSecret 泄漏。
- 无公开个人信息。
- 无未授权素材。
- 主要设备稳定。
- 包体符合提交时平台限制。
- 关键性能预算通过。
- 无 P0/P1 缺陷。
- P2 缺陷有明确接受记录。

## 42.4 发布完成

- 主体、出版、审批、备案、防沉迷、隐私和微信审核要求已由具备资格的人员确认。
- 用户协议和隐私政策与实际数据处理一致。
- 账户注销和数据删除实测通过。
- 第三方 SDK 清单准确。
- 客服、投诉、告警和应急流程可用。
- 正式素材台账完整。
- 公共名称通过商标和近似名审查。

---

# 43. Codex 工作协议

每次工作循环：

1. 阅读：
   - `AGENTS.md`
   - `docs/status/PROJECT_STATUS.md`
   - `docs/status/IMPLEMENTATION_PLAN.md`
   - 最近 ADR
   - 当前测试失败
2. 选择当前最高优先级、无外部阻塞的完整任务。
3. 先补测试或明确验收。
4. 实现。
5. 运行最小相关测试。
6. 运行跨包影响测试。
7. 更新文档和状态。
8. 记录新风险。
9. 继续下一个任务，直到阶段 Gate 满足。

禁止的工作方式：

- 只创建目录和空文件。
- 用静态假数据伪装真实后端。
- 用 `Math.random()` 临时实现核心随机。
- 用 Cocos 刚体作为权威战斗结果。
- 把秘密写入代码。
- 跳过迁移。
- 直接修改生产内容。
- 忽略失败测试。
- 为通过测试删除断言。
- 大面积降低类型安全。
- 将占位素材标为正式资源。
- 复制竞品名称和素材。

---

# 44. 必须维护的文档

```text
docs/status/
├─ PROJECT_STATUS.md
├─ IMPLEMENTATION_PLAN.md
├─ KNOWN_ISSUES.md
├─ EXTERNAL_BLOCKERS.md
└─ RELEASE_READINESS.md

docs/architecture/
├─ SYSTEM_OVERVIEW.md
├─ DETERMINISTIC_BATTLE.md
├─ TERRAIN_SYSTEM.md
├─ SAVE_AND_REPLAY.md
├─ SERVER_MODULES.md
├─ CONTENT_PIPELINE.md
└─ PVP_ARCHITECTURE.md

docs/design/
├─ GAME_DESIGN.md
├─ ROBOTS.md
├─ MODULES.md
├─ ENEMIES.md
├─ BOSSES.md
├─ EXPEDITION.md
├─ DAILY_CHALLENGE.md
└─ BALANCE_PRINCIPLES.md

docs/testing/
├─ TEST_STRATEGY.md
├─ DEVICE_MATRIX.md
├─ PERFORMANCE_BUDGET.md
└─ RELEASE_REGRESSION.md
```

所有重大架构改变必须新建 ADR：

- 背景。
- 决策。
- 备选方案。
- 后果。
- 迁移影响。
- 回滚方式。

---

# 45. 外部阻塞项

Codex 无法自行完成但必须建立流程的事项：

- 微信小游戏 AppID 和主体认证。
- 平台登录密钥。
- 正式域名和 TLS。
- 生产云资源。
- 网络游戏审批、出版、备案和防沉迷接入。
- 专业法律审查。
- 正式商标检索和申请。
- 正式手绘美术。
- 正式音乐和音效授权。
- 真实设备大规模兼容测试。
- 微信正式提交审核。

对这些事项：

- 不得伪造凭证。
- 不得绕过审核。
- 不得在仓库保存真实秘密。
- 应提供 Mock、适配器、文档、验证脚本和清晰接入点。
- 应在 `EXTERNAL_BLOCKERS.md` 中精确列出负责人、所需输入、完成标准和影响阶段。

---

# 46. 初始实现优先级

Codex 第一次进入空仓库时，应按以下顺序开始：

1. 建立 Monorepo、工具链和状态文档。
2. 创建确定性运行时包并完成测试。
3. 建立 Battle Command/Event/Snapshot/Replay 基础模型。
4. 实现地形栅格、材料、支撑和坍塌。
5. 实现回合、能源、生命和结构损伤。
6. 实现模块系统和首批完整模块。
7. 接入最小 Cocos 战斗表现层。
8. 扩展至全部模块、敌人、Boss 和远征。
9. 实现服务端、存档、挑战和排行榜。
10. 实现内容工具和后台。
11. 完成 V1.0 Gate。
12. 再开始 V2.0 PvP。

不要先花大量时间制作主界面视觉，而忽略确定性内核。

---

# 47. 官方参考资料

在实现和发布前重新核验最新版本和规则。

- Cocos Creator 3.8 微信小游戏文档：  
  https://docs.cocos.com/creator/3.8/manual/zh/editor/publish/publish-wechatgame.html
- Cocos Creator 3.8 微信引擎插件：  
  https://docs.cocos.com/creator/3.8/manual/zh/editor/publish/wechatgame-plugin.html
- 微信小游戏开发文档：  
  https://developers.weixin.qq.com/minigame/dev/
- Node.js 版本支持状态：  
  https://nodejs.org/en/about/previous-releases
- NestJS 文档：  
  https://docs.nestjs.com/
- 国家新闻出版署游戏审批结果：  
  https://www.nppa.gov.cn/bsfw/jggs/yxspjg/
- 《中华人民共和国个人信息保护法》：  
  https://www.cac.gov.cn/2021-08/20/c_1631050028355286.htm
- 国家版权局《中华人民共和国著作权法》：  
  https://www.ncac.gov.cn/xxfb/flfg/flfg_532/202103/t20210309_50530.html
- 中国商标网：  
  https://sbj.cnipa.gov.cn/

---

# 48. 最终指令

持续实现，直至达到对应阶段的 Definition of Done。

“可以运行”不等于完成。  
“有一个关卡”不等于完整 PvE。  
“客户端显示了排行榜”不等于成绩可信。  
“用了原创名称”不等于没有侵权风险。  
“没有主动收集手机号”不等于隐私合规。  
“写了测试”不等于确定性已经成立。  
“留好接口”不等于功能已经实现。

任何阶段都必须以可验证的交付物、自动化测试、真实文档和明确 Gate 作为完成证据。
