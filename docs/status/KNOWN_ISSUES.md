# Known Issues

Last reviewed on 2026-07-25. No open P0 issue remains; `PKG-001` is an open P1
release blocker.

## Open

| ID       | Severity | Area           | Description                                                                                                                                                                                                             | Disposition                                                                                                                                               |
| -------- | -------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PKG-001  | P1       | WeChat package | The successful Cocos build produces a 6,102,124-byte main package, exceeding the repository's 1,887,436-byte internal main-package budget; the two declared feature subpackages currently contribute no compiled bytes. | Split client code/assets into effective subpackages and evaluate the WeChat engine plugin only with current authoritative platform evidence before an RC. |
| ART-001  | P2       | Assets         | Release-quality original artwork and audio are not present.                                                                                                                                                             | Tracked as an external release blocker; development placeholders must remain clearly marked and cannot enter `assets/release`.                            |
| NAME-001 | P2       | Brand          | Public product name has not completed trademark and platform similarity review.                                                                                                                                         | Continue using working identifiers internally only.                                                                                                       |

## Closed

| ID      | Severity | Area                | Description                                                                                                                                                                 | Resolution                                                                                                                                                        |
| ------- | -------- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DET-001 | P1       | Determinism tooling | The authority scanner followed package-local `node_modules` links and reported forbidden APIs from dependencies.                                                            | Restricted scanning to owned `src/` trees and added regression coverage for dependency and build outputs.                                                         |
| DET-002 | P1       | Canonical state     | Sparse arrays could serialize to invalid JSON-shaped output instead of failing closed.                                                                                      | Canonical serialization now rejects holes explicitly and has a regression test.                                                                                   |
| DET-003 | P1       | Replay contract     | The first reducer contract did not expose the replay root seed and the golden reducer therefore did not bind to it.                                                         | Added immutable replay context, seed-sensitive golden state, checkpoint events, and a seed-change test.                                                           |
| CI-001  | P1       | Server CI           | Protocol changes triggered server integration before the Phase 7 server application and scripts existed.                                                                    | The workflow now provisions PostgreSQL, applies the real migration, and runs HTTP/database integration whenever server-owned paths change.                        |
| DEV-001 | P2       | Local tooling       | Docker CLI and the local state services were unavailable on the workstation.                                                                                                | Docker Desktop is installed; PostgreSQL 17.5, Redis 8.0.2 and MinIO are healthy, migrations apply, and available integration checks pass.                         |
| DEV-002 | P2       | Client tooling      | Cocos Creator 3.8.8 and WeChat Developer Tools were unavailable on the workstation.                                                                                         | Both tools are installed; login, sandbox import, compile, landscape simulator startup, and a real Creator build are confirmed.                                    |
| DEV-003 | P1       | Client runtime      | Cocos lowered array spread over `Set`, `Map` and generic iterables into invalid array concatenation in the WeChat runtime, preventing battle creation and terrain mutation. | Replaced affected client-shipped spreads with `Array.from`, added focused battle coverage, rebuilt, and completed a real game-context fire-to-victory smoke test. |
| DEV-004 | P2       | Client rendering    | Sharing root-level `Graphics` components allowed the battle background to cover terrain and actors.                                                                         | Moved background, terrain and aiming graphics onto ordered child nodes and confirmed the complete battlefield in the simulator.                                   |
| UX-001  | P2       | Touch controls      | The first local-battle control row used small, visually uniform buttons and implicit increment actions, producing weak touch feedback.                                      | Added explicit plus/minus controls, larger touch targets, press-scale feedback, semantic colors and settings-aware vibration; verified the rebuilt UI at 844x390. |
| TER-001 | P1       | Terrain anchors     | Treating every detached root as a malformed runtime state prevented deterministic destruction and later repair.                                                             | Creation and map validation require attachment; runtime analysis skips destroyed roots while preserving them for repair.                                          |

P0 and P1 issues block phase completion and merge. P2 acceptance requires an explicit owner, impact, and release disposition.
