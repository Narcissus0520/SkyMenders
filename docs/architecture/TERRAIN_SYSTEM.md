# Terrain System

Status: planned for Phase 2.

The authority model will use a chunked logical grid, material properties, fixed anchors, local support connectivity, deterministic collapse queues, and canonical state hashing. Render contours and debris are presentation outputs and cannot feed authoritative state. Dirty chunks bound recomputation; full-map per-frame analysis is prohibited.
