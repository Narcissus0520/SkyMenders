{
  "name": "@skymenders/challenge-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src test benchmark",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:determinism": "vitest run test/challenge.test.ts",
    "test:performance": "tsx benchmark/challenge-benchmark.ts && tsx benchmark/verification-benchmark.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@skymenders/ai-core": "workspace:*",
    "@skymenders/battle-core": "workspace:*",
    "@skymenders/content-runtime": "workspace:*",
    "@skymenders/content-schema": "workspace:*",
    "@skymenders/deterministic-runtime": "workspace:*",
    "@skymenders/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "eslint": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
